# Return the list of selected file names:
print(fileNames)
}
readSites(c(4, 79, 161))
readSites <- function(stations, weather_variable) {
# Get all the filenames from the directory:
fileNames = list.files(path="./Data/", pattern = "*.csv")
# Remove the Sites.csv file as we are not using it for this function:
fileNames <- fileNames[fileNames != "Sites.csv"]
# Get the file numbers from their names:
fileNumbers <- as.numeric(gsub(".*?([0-9]+).*", "\\1", fileNames))
# Use the two vectors to convert fileNames into a key-value set:
names(fileNames) <- fileNumbers
for (i in stations) {
print(fileNames[str(i)])
}
# Return the list of selected file names:
print(fileNames["1090"])
}
readSites(c(4, 79, 161))
readSites <- function(stations, weather_variable) {
# Get all the filenames from the directory:
fileNames = list.files(path="./Data/", pattern = "*.csv")
# Remove the Sites.csv file as we are not using it for this function:
fileNames <- fileNames[fileNames != "Sites.csv"]
# Get the file numbers from their names:
fileNumbers <- as.numeric(gsub(".*?([0-9]+).*", "\\1", fileNames))
# Use the two vectors to convert fileNames into a key-value set:
names(fileNames) <- fileNumbers
for (i in stations) {
print(fileNames[str(i)])
}
# Return the list of selected file names:
# print(fileNames["49"])
}
readSites(c(4, 79, 161))
readSites <- function(stations, weather_variable) {
# Get all the filenames from the directory:
fileNames = list.files(path="./Data/", pattern = "*.csv")
# Remove the Sites.csv file as we are not using it for this function:
fileNames <- fileNames[fileNames != "Sites.csv"]
# Get the file numbers from their names:
fileNumbers <- as.numeric(gsub(".*?([0-9]+).*", "\\1", fileNames))
# Use the two vectors to convert fileNames into a key-value set:
names(fileNames) <- fileNumbers
for (i in stations) {
print(fileNames[str(i)])
}
# Return the list of selected file names:
# print(fileNames["4"])
}
readSites(c(4, 79, 161))
readSites <- function(stations, weather_variable) {
# Get all the filenames from the directory:
fileNames = list.files(path="./Data/", pattern = "*.csv")
# Remove the Sites.csv file as we are not using it for this function:
fileNames <- fileNames[fileNames != "Sites.csv"]
# Get the file numbers from their names:
fileNumbers <- as.numeric(gsub(".*?([0-9]+).*", "\\1", fileNames))
# Use the two vectors to convert fileNames into a key-value set:
names(fileNames) <- fileNumbers
for (i in stations) {
print(fileNames[str(i)])
}
# Return the list of selected file names:
# print(fileNames["1090"])
}
readSites(c(4, 79, 161))
readSites <- function(stations, weather_variable) {
# Get all the filenames from the directory:
fileNames = list.files(path="./Data/", pattern = "*.csv")
# Remove the Sites.csv file as we are not using it for this function:
fileNames <- fileNames[fileNames != "Sites.csv"]
# Get the file numbers from their names:
fileNumbers <- as.numeric(gsub(".*?([0-9]+).*", "\\1", fileNames))
# Use the two vectors to convert fileNames into a key-value set:
names(fileNames) <- fileNumbers
for (i in stations) {
print(fileNames[str(i)])
}
# Return the list of selected file names:
print(fileNames["4"])
}
readSites(c(4, 79, 161))
readSites <- function(stations, weather_variable) {
# Get all the filenames from the directory:
fileNames = list.files(path="./Data/", pattern = "*.csv")
# Remove the Sites.csv file as we are not using it for this function:
fileNames <- fileNames[fileNames != "Sites.csv"]
# Get the file numbers from their names:
fileNumbers <- as.numeric(gsub(".*?([0-9]+).*", "\\1", fileNames))
# Use the two vectors to convert fileNames into a key-value set:
names(fileNames) <- fileNumbers
for (i in stations) {
print(str(i))
}
# Return the list of selected file names:
# print(fileNames["4"])
}
readSites(c(4, 79, 161))
readSites <- function(stations, weather_variable) {
# Get all the filenames from the directory:
fileNames = list.files(path="./Data/", pattern = "*.csv")
# Remove the Sites.csv file as we are not using it for this function:
fileNames <- fileNames[fileNames != "Sites.csv"]
# Get the file numbers from their names:
fileNumbers <- as.numeric(gsub(".*?([0-9]+).*", "\\1", fileNames))
# Use the two vectors to convert fileNames into a key-value set:
names(fileNames) <- fileNumbers
for (i in stations) {
print(str(i))
}
# Return the list of selected file names:
# print(fileNames["4"])
}
readSites(c("4", "79", "161"))
readSites <- function(stations, weather_variable) {
# Get all the filenames from the directory:
fileNames = list.files(path="./Data/", pattern = "*.csv")
# Remove the Sites.csv file as we are not using it for this function:
fileNames <- fileNames[fileNames != "Sites.csv"]
# Get the file numbers from their names:
fileNumbers <- as.numeric(gsub(".*?([0-9]+).*", "\\1", fileNames))
# Use the two vectors to convert fileNames into a key-value set:
names(fileNames) <- fileNumbers
for (i in stations) {
print(i)
}
# Return the list of selected file names:
# print(fileNames["4"])
}
readSites(c("4", "79", "161"))
readSites <- function(stations, weather_variable) {
# Get all the filenames from the directory:
fileNames = list.files(path="./Data/", pattern = "*.csv")
# Remove the Sites.csv file as we are not using it for this function:
fileNames <- fileNames[fileNames != "Sites.csv"]
# Get the file numbers from their names:
fileNumbers <- as.numeric(gsub(".*?([0-9]+).*", "\\1", fileNames))
# Use the two vectors to convert fileNames into a key-value set:
names(fileNames) <- fileNumbers
for (i in stations) {
print(fileNames[i])
}
# Return the list of selected file names:
# print(fileNames["4"])
}
readSites(c("4", "79", "161"))
readSites <- function(stations, weather_variable) {
# Get all the filenames from the directory:
fileNames = list.files(path="./Data/", pattern = "*.csv")
# Remove the Sites.csv file as we are not using it for this function:
fileNames <- fileNames[fileNames != "Sites.csv"]
# Get the file numbers from their names:
fileNumbers <- as.numeric(gsub(".*?([0-9]+).*", "\\1", fileNames))
# Use the two vectors to convert fileNames into a key-value set:
names(fileNames) <- fileNumbers
# Form a list of files to read:
files <- c()
for (i in stations) {
files <- c(files, fileNames[i])
}
# Return the list of selected file names:
# return(files)
}
print(readSites(c("4", "79", "161")))
readSites(c("4", "79", "161"))
readSites <- function(stations, weather_variable) {
# Get all the filenames from the directory:
fileNames = list.files(path="./Data/", pattern = "*.csv")
# Remove the Sites.csv file as we are not using it for this function:
fileNames <- fileNames[fileNames != "Sites.csv"]
# Get the file numbers from their names:
fileNumbers <- as.numeric(gsub(".*?([0-9]+).*", "\\1", fileNames))
# Use the two vectors to convert fileNames into a key-value set:
names(fileNames) <- fileNumbers
# Form a list of files to read:
files <- c()
for (i in stations) {
files <- c(files, fileNames[i])
}
# Return the list of selected file names:
return(files)
}
readSites(c("4", "79", "161"))
runApp()
runApp()
runApp()
runApp()
runApp()
# Define the function to read the sites defined by the user:
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
sites_df <- ""
for (f in stations) {
sites_df <- rbind(data,read.csv(paste("./Data", f, sep = "/")))
# sites_df <- paste("./Data", f, sep = "/")
}
return(sites_df)
}
readFiles(c("4", "79", "161"))
# Define the function to read the sites defined by the user:
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
sites_df <- ""
for (f in stations) {
sites_df <- rbind(data,read.csv(paste("./Data", f, sep = "/")))
# sites_df <- paste("./Data/", f)
}
return(sites_df)
}
readFiles(c("4", "79", "161"))
# Define the function to read the sites defined by the user:
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
directory <- getwd()
directory <- passwordInput(directory, "/Data")
sites_df <- ""
for (f in stations) {
sites_df <- rbind(data,read.csv(paste(directory, f, sep = "/")))
# sites_df <- paste("./Data/", f)
}
return(sites_df)
}
readFiles(c("4", "79", "161"))
# Define the function to read the sites defined by the user:
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
directory <- getwd()
directory <- passwordInput(directory, "/Data")
sites_df <- ""
for (f in stations) {
sites_df <- rbind(data,read.csv(paste(directory, f, sep = "/")))
# sites_df <- paste("./Data/", f)
}
return(sites_df)
}
# Define the function to read the sites defined by the user:
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
directory <- getwd()
directory <- passwordInput(directory, "/Data")
print(directory)
sites_df <- ""
for (f in stations) {
sites_df <- rbind(data,read.csv(paste(directory, f, sep = "/")))
# sites_df <- paste("./Data/", f)
}
return(sites_df)
}
readFiles(c("4", "79", "161"))
# Define the function to read the sites defined by the user:
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
directory <- "./Data"
sites_df <- ""
for (f in stations) {
sites_df <- rbind(data,read.csv(paste(directory, f, sep = "/")))
print(paste(directory, f, sep = "/")
# sites_df <- paste("./Data/", f)
}
return(sites_df)
}
# Define the function to read the sites defined by the user:
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
directory <- "./Data"
sites_df <- ""
for (f in stations) {
sites_df <- rbind(data,read.csv(paste(directory, f, sep = "/")))
print(paste(directory, f, sep = "/"))
# sites_df <- paste("./Data/", f)
}
return(sites_df)
}
readFiles(c("4", "79", "161"))
# Define the function to read the sites defined by the user:
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
directory <- "./Data"
sites_df <- ""
for (f in stations) {
# sites_df <- rbind(data,read.csv(paste(directory, f, sep = "/")))
print(paste(directory, f, sep = "/"))
# sites_df <- paste("./Data/", f)
}
# return(sites_df)
}
readFiles(c("4", "79", "161"))
# Define the function to read the sites defined by the user:
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
directory <- "./Data"
sites_df <- ""
for (f in stations) {
# sites_df <- rbind(data,read.csv(paste(directory, f, sep = "/")))
print(paste(directory, stations[f], sep = "/"))
# sites_df <- paste("./Data/", f)
}
# return(sites_df)
}
readFiles(c("4", "79", "161"))
# Define the function to read the sites defined by the user:
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
directory <- "./Data"
sites_df <- ""
for (f in stations) {
# sites_df <- rbind(data,read.csv(paste(directory, f, sep = "/")))
# print(paste(directory, stations[f], sep = "/"))
# sites_df <- paste("./Data/", f)
print(f)
}
# return(sites_df)
}
readFiles(c("4", "79", "161"))
# Define the function to read the sites defined by the user:
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
directory <- "./Data"
sites_df <- ""
for (f in stations) {
# sites_df <- rbind(data,read.csv(paste(directory, f, sep = "/")))
# print(paste(directory, stations[f], sep = "/"))
# sites_df <- paste("./Data/", f)
print(stations[f])
}
# return(sites_df)
}
readFiles(c("4", "79", "161"))
# Define the function to read the sites defined by the user:
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
directory <- "./Data"
sites_df <- ""
for (f in stations) {
# sites_df <- rbind(data,read.csv(paste(directory, f, sep = "/")))
# print(paste(directory, stations[f], sep = "/"))
# sites_df <- paste("./Data/", f)
print(stations)
}
# return(sites_df)
}
readFiles(c("4", "79", "161"))
runApp()
runApp()
runApp()
getFileNames <- function(stations) {
# Get all the filenames from the directory:
fileNames = list.files(path="./Data/", pattern = "*.csv")
# Remove the Sites.csv file as we are not using it for this function:
fileNames <- fileNames[fileNames != "Sites.csv"]
# Get the file numbers from their names:
fileNumbers <- as.numeric(gsub(".*?([0-9]+).*", "\\1", fileNames))
# Use the two vectors to convert fileNames into a key-value set:
names(fileNames) <- fileNumbers
# Form a list of files to read:
files <- c()
for (i in stations) {
files <- c(files, fileNames[i])
}
# Return the list of selected file names:
return(files)
}
files <- getFileNames(c("4", "79", "161"))
readFiles <- function(stations, weather_variable) {
# sites_df <- do.call(rbind, lapply(paste("./Data", stations, sep = "/"), read.csv))
path="./Data"
weather_df <- do.call(rbind, lapply(paste(path, stations, sep="/"), read.csv))
return(weather_df)
}
readFiles(files)
df <- readFiles(files)
View(df)
View(readFiles)
runApp()
runApp()
runApp()
shiny::runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
View(df)
df2 < df
df2 <- df
df2 %<>% mutate(df2["ob_time"]=as.Date(as.POSIXct(ob_time, format="%d/%m/%Y")))
df2 %<>% mutate(ob_time=as.Date(as.POSIXct(ob_time, format="%d/%m/%Y")))
df2
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
shiny::runApp()
runApp()
runApp()
runApp()
df2
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
shiny::runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
shiny::runApp()
shiny::runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
View(df)
runApp()
runApp()
